# Server Configuration
PORT=8081
BACKEND_URL=http://localhost:8081
FRONTEND_URL=http://localhost:3000

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_NAME=devplus
DB_SSLMODE=disable

# GitHub OAuth Configuration
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_REDIRECT_URI=http://localhost:8081/api/v1/auth/github/callback

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters

# Kestra Configuration
KESTRA_URL=http://localhost:8080
KESTRA_USERNAME=admin
KESTRA_PASSWORD=kestra

# Environment
ENVIRONMENT=development

# Kestra Secrets (base64 encoded)
# Encode with: echo -n "your_api_key" | base64
SECRET_GEMINI_API_KEY=your_base64_encoded_gemini_api_key_here
